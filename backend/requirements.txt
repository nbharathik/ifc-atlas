fastapi==0.140.0
starlette==1.3.1
uvicorn[standard]==0.34.0
python-multipart==0.0.32
ifcopenshell>=0.8.3
pydantic==2.13.4
websockets==15.0.1
httpx==0.28.1
python-dotenv==1.2.2
anthropic==0.109.1
openai==2.41.0
# SDK majors match the LangChain 1.x integrations and the Responses API path.
langgraph==1.2.9
# Keep prebuilt in the resolver-compatible line with LangGraph. Earlier
# combinations resolved incompatible ExecutionInfo imports and silently
# downgraded chat turns to the fallback streamers.
langgraph-prebuilt==1.1.0
langchain==1.3.9
langchain-openai==1.1.16
langchain-anthropic==1.4.8
# Explicit security floor for the transitive tracing client.
langsmith==0.8.18
mcp==1.28.1
gitpython>=3.1,<4
# Semantic IFC diff for the history timeline (GET /api/ifc/history/diff).
# Maintained alongside ifcopenshell (same release train); pure Python + deepdiff.
ifcdiff>=0.8,<0.9
# IDS 1.0 validation (buildingSMART reference engine). ids_service falls back
# to a minimal v0 parser without it, but the full facet engine + the IDS test
# suite require it.
ifctester>=0.8
# PDF text extraction for the document index. document_index_service degrades
# to raw-text handling without it, which produces useless PDF snippets.
pypdf>=5,<7

# Local semantic search for document_index_service + the reference-docs RAG
# (E2/E3). Promoted to standard deps (ADR-era editor plan E5): offline
# BM25+semantic hybrid search, no API key. The imports in
# document_index_service stay guarded so a stripped env still runs (BM25-only).
# fastembed downloads a ~25 MB ONNX model (BAAI/bge-small-en-v1.5) on FIRST USE,
# not at install - see document_index_service for the first-use download.
#
# Desktop sidecar note (plan D6): the PyInstaller freeze (scripts/build_sidecar.*)
# must EXCLUDE these - onnxruntime bloats the frozen exe and the desktop build
# is fine on BM25-only. Add `--exclude-module fastembed --exclude-module
# onnxruntime --exclude-module hnswlib` when that workstream lands.
fastembed>=0.8
hnswlib>=0.8
