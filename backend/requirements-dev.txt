# Test/dev-only dependencies. Runtime dependencies live in requirements.txt.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
pytest>=8,<9
pytest-asyncio>=0.24,<2
ruff==0.16.0
pip-audit==2.10.1
