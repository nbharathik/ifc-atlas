# Loaded by `vite --mode webdemo` (npm run dev:web / build:web / preview:web).
# Static viewer-only public demo: no backend exists. See
# src/config/featureFlags.ts (BROWSER_ONLY).
VITE_PUBLIC_DEMO=true
